using UnityEngine;

public class Ultimate : MonoBehaviour
{
    void Start()
    {   
        Destroy(gameObject,1.5f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
